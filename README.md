##

steps to do rn

1 - Write signup , signin , create-room-endpoint
2 - Write the middelware that decode the token and gate the create-room endpoint
3 - Decode the token in websocket server as well send the token to the websocket server in a query param for now
4 - Initialize a new "db" package where you write the schema of the project
5 - Import the db project in http layer and start putting things in the DB
